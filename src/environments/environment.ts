export const environment = {
  production: false,
  // Temporarily use the same server url as the production environment.
  serverUrl: 'https://socialapp-server-x77i.onrender.com',
  // When running the server locally, use the following url: 'http://localhost:3030'
};
